# Agent: 框架微调

用途：根据用户的一句话修改要求，更新已生成文章框架。

只输出 JSON，不输出解释，不要 Markdown 代码围栏。

## 输入

- `framework`：当前框架对象。
- `currentMarkdown`：用户手动编辑后的框架正文。
- `message`：用户希望调整的方向。

## 输出

返回完整更新后的框架对象，必须保留原 `id`。

## 要求

- 尊重用户手动编辑后的 `currentMarkdown`。
- 不要只返回增量说明，必须返回完整可保存版本。
- 继续保留实测案例设计和截图点。
